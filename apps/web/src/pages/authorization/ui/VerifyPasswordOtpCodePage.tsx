import { VerifyPasswordOtpForm } from "@modules/auth";

export const VerifyPasswordOtpCodePage = () => {
  return (
    <div className="relative flex w-full flex-1 items-center justify-center">
      <VerifyPasswordOtpForm />
    </div>
  );
};
