generator client {
  provider = "prisma-client"
  output   = "../src/libs/prisma"

  runtime = "nodejs"
  moduleFormat = "cjs"
  generatedFileExtension = "ts"
}

datasource db {
  provider = "postgresql"
}